# 개발: 비워 두면 Vite 개발 프록시(vite.config.ts)를 통해 same-origin 으로 요청합니다.
# 배포: 실제 백엔드 origin 을 넣고, 백엔드의 HUB_CORS_ALLOWED_ORIGINS 에도 등록하세요.
VITE_API_BASE_URL=

# 백엔드 없이 UI 만 확인할 때 .env.local 에 넣어 사용합니다 (커밋 금지).
# VITE_DEV_AUTH_BYPASS=true
